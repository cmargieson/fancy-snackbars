version: 2.1

orbs:
  node: circleci/node@3.0.1

jobs:

  test:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: 
          name: Run NPM tests
          command: |
            npm install
            npm run test

  publish:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run:
          name: Publish to NPM
          command: |
            npm set //registry.npmjs.org/:_authToken=$NPM_TOKEN
            npm publish

workflows:
  build-and-deploy:
    jobs:
      - test
          filters:
            tags:
              only: /.*/

      - publish:
          requires:
            - node/test
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v.*/